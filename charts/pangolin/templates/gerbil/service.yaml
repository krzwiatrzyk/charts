apiVersion: v1
kind: Service
metadata:
  name: gerbil
spec:
  type: ClusterIP
  ports:
    {{- range .Values.gerbil.ports }}
    - name: {{ .name }}
      port: {{ .containerPort }}
      targetPort: {{ .containerPort }}
      protocol: {{ .protocol | default "TCP" }}
    {{- end }}
  selector:
    app: gerbil